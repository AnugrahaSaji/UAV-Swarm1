---
description: Wrapper skill: validate scheduler control-plane lifecycle and ordering (canonical skill lives under .github/skills)
---

# scheduler-control-validation

This is a **repo-local wrapper** so the skill is discoverable via `secure-tunnel/skills/`.

**Canonical definition:** `secure-tunnel/.github/skills/scheduler-control-validation/SKILL.md`

## Usage
- Read the canonical definition and follow it.
- Keep findings evidence-backed (code paths, logs, datasets).

